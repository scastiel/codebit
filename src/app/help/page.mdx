export const metadata = {
  title: 'Help'
}

# How to use CodeBit

In CodeBit, a code animation is called a **snippet**, which contains several **steps**, i.e. several versions of the code.

- The animation will display one step after the other;
- The transition between each step is based on a _diff_ algorithm, simulating key strokes.

## Format of a snippet definition

Here is an example of a snippet definition:

`````markdown
---
theme: dark
background: 60665
---

```ts
console.log("Hello World!")
```

---

```ts
console.log("Hello Amazing World!")
```
`````

Notice that:

1. The syntax is plain Markdown
2. It includes some configuration in the _frontmatter_ (the part between `---` in the header)
3. Each step is a code block delimited by `` ```ts `` (`ts` is for TypeScript; feel free to try different languages) and separated with `---`.

You can specify a file name on each code block, that will be displayed at top. If the feature is available with the plan you subscribed to, you can define several files in your snippets, creating an animation when switching from one file to another.

`````md
```ts filename="hello.ts"
console.log("Hello World!")
```
`````

## Configuration options

- `theme` can be either `light` or `dark` (default)
- `background` is a number used to generate a random background mesh gradient. Try different values to find the best one.
{/* - `speed` let you change the speed of the animation. The default is `1`; `2` will be twice faster, `0.5` half slower, etc. */}
- `watermark` controls wether a reference to CodeBit is displayed at the bottom. It is `true` by default, and can be `false` for paying users only.

## Some tips for better animations

Creating different versions of the same code for animation can be exhausting, as a tiny change at the beginning forces you to change every step after.

Here are a few tips we discovered when using our application to make it easier:

1. Start with the **first** and the **last** steps of your animation, then continue with intermediate steps of your explanation, and end with steps that are here only for animation tweaking.

2. If the animation isn’t exactly the one you want, add extra steps. For instance, if a steps adds a line of code between two lines, it might look nicer if you first add an empty line, then add the line content:

`````markdown
```ts
first()
third()
```

---

```ts
first()

third()
```

---

```ts
first()
second()
third()
```
`````

## Anything unclear? Do you have other tips?

We’re constantly working on improving the snippet creation experience. If anything is unclear or if you want to share any other tips, shout us an email at [hello@codebit.xyz](mailto:hello@codebit.xyz).